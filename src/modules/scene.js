/**
 * Create scene and export function
 */

// Imports
import { Scene } from 'three';

// Create scene
export const createScene =()=> {

  // Instaniate scene
  const scene = new Scene();

  return scene;
}

export default createScene;